# Verified email change validation scope

The phase must pass the existing Theme and storefront safety workflow before merge.

Compiler-sensitive coverage includes:

- public confirmation route rendering
- protected profile workspace rendering
- account API imports and action branches
- PostgreSQL raw-query typing
- tenant/store/customer scoping
- v0 protected account-slot boundaries
- installed theme registry validation
- folder and ZIP theme installer dry runs
- complete Next.js production build
