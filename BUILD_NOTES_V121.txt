v121 full codebase corruption fix

- Ran full codebase corruption sweep across .ts/.tsx/.js/.jsx
- Fixed known token corruption patterns like:
  - imp||t -> import
  - exp||t -> export
  - b||der -> border
  - b||derColor -> borderColor
  - col|| -> color
  - backg||und -> background
- Intended as a JSX-safe stabilization build only
- No new features added
