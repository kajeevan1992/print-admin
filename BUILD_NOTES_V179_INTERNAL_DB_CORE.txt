Build v179 - Internal DB/Core hardening

Scope respected:
- Backend/core only.
- No UI changes.
- No public API changes.
- Reused existing internal routes and unified-core modules.

What changed:
1. Added real tenant DB resolver:
   - src/core/db/tenant-prisma.ts
   - src/core/db/tenant-db-resolver.ts
   Resolves tenant/site/databaseConnectionId to the saved TenantDatabaseConnection and creates a per-tenant Prisma client using that tenant database URL.

2. Replaced placeholder tenant DB setup:
   - src/core/db/tenant-db-manager.ts
   - src/core/db/tenant-db-setup.ts
   Setup now performs live pg connection checks and creates tenant schema tables required for internal core catalog reads/writes.

3. Replaced placeholder backup hook:
   - src/core/db/backup-manager.ts
   Runs pg_dump when available, otherwise creates a JSON export of tenant core tables. Backups write to PLATFORM_DB_BACKUP_DIR or /tmp/print-platform-db-backups.

4. Added real internal catalog DB reads and reusable CRUD core logic:
   - src/core/catalog/internal-catalog.service.ts
   Products/categories read/write real Product and Category tables.
   Materials/finishes/collections/tags/option-sets read/write CoreCatalogRecord.
   Demo data remains only as safe fallback when no tenant DB is configured/ready.

5. Added tenant request context helper:
   - src/core/tenant/context.ts
   Internal catalog/status routes now use tenantId/siteId/databaseConnectionId from query params or x-* headers.

6. Added CoreCatalogRecord Prisma model + migration:
   - prisma/schema.prisma
   - prisma/migrations/20260424140500_core_catalog_record/migration.sql

Validation note:
- Could not complete npm install/build in this sandbox because dependencies were not present in the uploaded zip and package installation timed out here.
- Code was kept TypeScript/Next compatible and limited to backend/core/internal route wiring.
