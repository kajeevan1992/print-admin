'use client';

export const dynamic = 'force-dynamic';

import { useEffect, useMemo, useState } from 'react';
import { PageHeader } from '@/components/ui/page-header';
import { Card } from '@/components/ui/card';
import { Button, PrimaryButton } from '@/components/ui/buttons';
import { Input } from '@/components/forms/input';

type StockStatus = 'Healthy' | 'Low' | 'Critical' | 'Inbound';
type LocationType = 'Warehouse' | 'Plant' | 'Materials';
type DbStatus = 'loading' | 'connected' | 'saving' | 'local' | 'error';
type JobTicket = { id: string; orderNumber: string; productName: string; quantity: number; status: string; machine?: string; material?: string; dueDate?: string; finishing?: string[]; warnings?: string[] };
type ConsumptionPlan = { ticketId: string; orderNumber: string; productName: string; materialSku: string; quantity: number; consumeUnits: number; wasteUnits: number; totalUnits: number; status: string; dueDate?: string };
type PurchaseOrder = { id: string; poNumber: string; supplier: string; sku: string; title: string; quantity: number; unitCostMinor: number; landedCostMinor: number; status: 'draft' | 'sent' | 'confirmed' | 'received'; eta: string; createdAt: string; notes: string };

type InventoryItem = { id: string; sku: string; title: string; location: string; locationType: LocationType; onHand: number; reserved: number; reorderPoint: number; incoming: number; leadDays: number; status: StockStatus; notes: string; materialType?: 'sheet' | 'roll' | 'board' | 'consumable'; unit?: 'sheets' | 'meters' | 'boards' | 'units'; linkedMaterialIds?: string[]; wastePercent?: number; preferredSupplier?: string; unitCostMinor?: number; reorderMultiple?: number };

const storageKey = 'inventory-control-v54';
const poStorageKey = 'inventory-purchase-orders-v373';
const inventoryEndpoint = `/api/internal/config/${encodeURIComponent(storageKey)}`;
const inventoryItemsEndpoint = `${inventoryEndpoint}/items`;
const poEndpoint = `/api/internal/config/${encodeURIComponent(poStorageKey)}`;
const poItemsEndpoint = `${poEndpoint}/items`;
const jobTicketsEndpoint = '/api/internal/config/production-job-tickets/items';

const seedInventory: InventoryItem[] = [
  { id: 'inv-001', sku: '350-SILK', title: '350gsm Silk SRA3 Sheets', location: 'Main Store', locationType: 'Materials', onHand: 1240, reserved: 320, reorderPoint: 500, incoming: 0, leadDays: 2, status: 'Healthy', notes: 'Primary stock for cards and flyers.', materialType: 'sheet', unit: 'sheets', linkedMaterialIds: ['350-silk', '350gsm-silk', 'card-silk-350'], wastePercent: 5, preferredSupplier: 'Paper Merchant', unitCostMinor: 18, reorderMultiple: 500 },
  { id: 'inv-002', sku: '170-SILK', title: '170gsm Silk SRA3 Sheets', location: 'Main Store', locationType: 'Materials', onHand: 180, reserved: 140, reorderPoint: 250, incoming: 400, leadDays: 4, status: 'Low', notes: 'Shared stock for brochure and flyer jobs.', materialType: 'sheet', unit: 'sheets', linkedMaterialIds: ['170-silk', '170gsm-silk'], wastePercent: 5, preferredSupplier: 'Paper Merchant', unitCostMinor: 11, reorderMultiple: 500 },
  { id: 'inv-003', sku: 'PVC-BANNER-1200', title: 'PVC Banner Roll 1200mm', location: 'Large Format Area', locationType: 'Materials', onHand: 52, reserved: 28, reorderPoint: 90, incoming: 180, leadDays: 7, status: 'Critical', notes: 'Roll stock for banners and signage.', materialType: 'roll', unit: 'meters', linkedMaterialIds: ['pvc-banner', 'banner', 'vinyl'], wastePercent: 8, preferredSupplier: 'Large Format Supplier', unitCostMinor: 250, reorderMultiple: 50 },
  { id: 'inv-004', sku: 'LAM-MATT', title: 'Matt Lamination Film', location: 'Finishing Bench', locationType: 'Plant', onHand: 420, reserved: 90, reorderPoint: 200, incoming: 250, leadDays: 3, status: 'Inbound', notes: 'Finishing consumable for laminated jobs.', materialType: 'consumable', unit: 'meters', linkedMaterialIds: ['matt', 'lamination', 'matt-lamination'], wastePercent: 3, preferredSupplier: 'Finishing Supplies', unitCostMinor: 38, reorderMultiple: 100 }
];
const emptyDraft: Omit<InventoryItem, 'id' | 'status'> = { sku: '', title: '', location: '', locationType: 'Warehouse', onHand: 0, reserved: 0, reorderPoint: 0, incoming: 0, leadDays: 3, notes: '', materialType: 'sheet', unit: 'sheets', linkedMaterialIds: [], wastePercent: 5, preferredSupplier: '', unitCostMinor: 0, reorderMultiple: 100 };

function computeStatus(item: Pick<InventoryItem, 'onHand' | 'reserved' | 'reorderPoint' | 'incoming'>): StockStatus { const available = Math.max(item.onHand - item.reserved, 0); if (available <= Math.max(1, Math.round(item.reorderPoint * 0.4))) return 'Critical'; if (available <= item.reorderPoint) return 'Low'; if (item.incoming > 0) return 'Inbound'; return 'Healthy'; }
function badgeClass(status: StockStatus | PurchaseOrder['status']) { if (status === 'Critical' || status === 'draft') return 'border-rose-500/20 bg-rose-500/10 text-rose-200'; if (status === 'Low' || status === 'sent') return 'border-amber-500/20 bg-amber-500/10 text-amber-200'; if (status === 'Inbound' || status === 'confirmed') return 'border-sky-500/20 bg-sky-500/10 text-sky-200'; return 'border-emerald-500/20 bg-emerald-500/10 text-emerald-200'; }
function toNumber(value: string, fallback = 0) { const parsed = Number(value); return Number.isFinite(parsed) ? parsed : fallback; }
function csv(value: string) { return value.split(',').map((item) => item.trim()).filter(Boolean); }
function csvJoin(value?: string[]) { return Array.isArray(value) ? value.join(', ') : ''; }
function money(minor = 0) { return new Intl.NumberFormat('en-GB', { style: 'currency', currency: 'GBP' }).format(minor / 100); }
function materialMatches(item: InventoryItem, material?: string) { const haystack = [item.sku, item.title, ...(item.linkedMaterialIds || [])].join(' ').toLowerCase(); return Boolean(material && haystack.includes(material.toLowerCase())); }
function estimateConsumption(item: InventoryItem, ticket: JobTicket) { const qty = Math.max(1, Number(ticket.quantity || 1)); if (item.materialType === 'roll') return Math.max(1, Math.ceil(qty / 10)); if (item.materialType === 'board') return Math.max(1, Math.ceil(qty / 2)); if (item.materialType === 'consumable') return Math.max(1, Math.ceil(qty / 15)); return Math.max(1, Math.ceil(qty / 20)); }
function eta(days: number) { const date = new Date(); date.setDate(date.getDate() + days); return date.toISOString().slice(0, 10); }
async function readTickets(): Promise<JobTicket[]> { try { const response = await fetch(jobTicketsEndpoint, { cache: 'no-store' }); const payload = await response.json().catch(() => ({})); return Array.isArray(payload.data?.items) ? payload.data.items : []; } catch { return []; } }
async function readPurchaseOrders(): Promise<PurchaseOrder[]> { try { const response = await fetch(poItemsEndpoint, { cache: 'no-store' }); const payload = await response.json().catch(() => ({})); return Array.isArray(payload.data?.items) ? payload.data.items : []; } catch { return []; } }
function buildConsumption(items: InventoryItem[], tickets: JobTicket[]): ConsumptionPlan[] { return tickets.filter((ticket) => !['dispatched', 'blocked'].includes(ticket.status)).flatMap((ticket) => items.filter((item) => materialMatches(item, ticket.material)).map((item) => { const consumeUnits = estimateConsumption(item, ticket); const wasteUnits = Math.ceil(consumeUnits * Number(item.wastePercent || 0) / 100); return { ticketId: ticket.id, orderNumber: ticket.orderNumber, productName: ticket.productName, materialSku: item.sku, quantity: Number(ticket.quantity || 1), consumeUnits, wasteUnits, totalUnits: consumeUnits + wasteUnits, status: ticket.status, dueDate: ticket.dueDate }; })); }
function reorderQty(item: InventoryItem, jobDemand = 0) { const available = Math.max(item.onHand + item.incoming - Math.max(item.reserved, jobDemand), 0); const target = Math.max(item.reorderPoint * 2, item.reorderPoint + jobDemand); const shortage = Math.max(0, target - available); const multiple = Math.max(1, item.reorderMultiple || 1); return Math.ceil(shortage / multiple) * multiple; }
function shouldReorder(item: InventoryItem, jobDemand = 0) { return reorderQty(item, jobDemand) > 0 && Math.max(item.onHand + item.incoming - Math.max(item.reserved, jobDemand), 0) <= Math.max(item.reorderPoint, jobDemand); }

export default function Page() {
  const [items, setItems] = useState<InventoryItem[]>(seedInventory);
  const [tickets, setTickets] = useState<JobTicket[]>([]);
  const [purchaseOrders, setPurchaseOrders] = useState<PurchaseOrder[]>([]);
  const [query, setQuery] = useState('');
  const [statusFilter, setStatusFilter] = useState<'All' | StockStatus>('All');
  const [locationFilter, setLocationFilter] = useState<'All' | LocationType>('All');
  const [activeId, setActiveId] = useState('');
  const [draft, setDraft] = useState(emptyDraft);
  const [editingId, setEditingId] = useState<string | null>(null);
  const [dbStatus, setDbStatus] = useState<DbStatus>('loading');
  const [dbMessage, setDbMessage] = useState('Loading inventory from internal API...');

  useEffect(() => { void loadInventory(); }, []);
  async function loadInventory() { setDbStatus('loading'); setDbMessage('Loading inventory, purchase orders and production demand...'); try { const [response, ticketRows, poRows] = await Promise.all([fetch(inventoryItemsEndpoint, { cache: 'no-store' }), readTickets(), readPurchaseOrders()]); const payload = await response.json().catch(() => ({})); const saved = payload?.data?.items; const next = Array.isArray(saved) && saved.length > 0 ? saved.map((item) => ({ ...item, status: computeStatus(item) })) as InventoryItem[] : seedInventory; setItems(next); setTickets(ticketRows); setPurchaseOrders(poRows); setActiveId(next[0]?.id ?? ''); setDbStatus('connected'); setDbMessage('Connected. Inventory, purchasing and production demand loaded.'); } catch (error) { const raw = window.localStorage.getItem(storageKey); if (raw) { try { const parsed = JSON.parse(raw) as InventoryItem[]; if (Array.isArray(parsed) && parsed.length > 0) { setItems(parsed); setTickets(await readTickets()); setPurchaseOrders(await readPurchaseOrders()); setActiveId(parsed[0]?.id ?? ''); setDbStatus('local'); setDbMessage(`Inventory API unavailable, showing browser fallback: ${error instanceof Error ? error.message : 'unknown error'}`); return; } } catch {} } setItems(seedInventory); setTickets(await readTickets()); setPurchaseOrders(await readPurchaseOrders()); setActiveId(seedInventory[0]?.id ?? ''); setDbStatus('error'); setDbMessage(error instanceof Error ? error.message : 'Inventory could not be loaded.'); } }
  async function persistInventory(next: InventoryItem[], message = 'Saving inventory through internal API...') { setDbStatus('saving'); setDbMessage(message); try { const response = await fetch(inventoryEndpoint, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ title: 'Inventory Control', description: 'Inventory stock pool records with v373 purchasing automation', items: next, values: { count: String(next.length), version: 'v373' } }) }); const payload = await response.json().catch(() => ({})); if (!response.ok || payload?.ok === false) throw new Error(payload?.error || 'Inventory save failed.'); window.localStorage.setItem(storageKey, JSON.stringify(next)); setDbStatus('connected'); setDbMessage('Saved to database through internal API.'); } catch (error) { window.localStorage.setItem(storageKey, JSON.stringify(next)); setDbStatus('local'); setDbMessage(`Database save failed, kept browser fallback copy: ${error instanceof Error ? error.message : 'unknown error'}`); } }
  async function persistPurchaseOrders(next: PurchaseOrder[], message = 'Saving purchase orders...') { setDbStatus('saving'); setDbMessage(message); try { await fetch(poEndpoint, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ title: 'Purchase Orders', description: 'Supplier reorder automation and inbound tracking', items: next, values: { count: String(next.length), version: 'v373' } }) }); setPurchaseOrders(next); setDbStatus('connected'); setDbMessage('Purchase orders saved.'); } catch (error) { setPurchaseOrders(next); setDbStatus('local'); setDbMessage(`PO save fallback only: ${error instanceof Error ? error.message : 'unknown error'}`); } }
  function setItemsAndPersist(next: InventoryItem[], message?: string) { setItems(next); void persistInventory(next, message); }

  const consumption = useMemo(() => buildConsumption(items, tickets), [items, tickets]);
  const consumptionBySku = useMemo(() => consumption.reduce((acc, row) => ({ ...acc, [row.materialSku]: (acc[row.materialSku] || 0) + row.totalUnits }), {} as Record<string, number>), [consumption]);
  const openPoBySku = useMemo(() => purchaseOrders.filter((po) => po.status !== 'received').reduce((acc, po) => ({ ...acc, [po.sku]: (acc[po.sku] || 0) + po.quantity }), {} as Record<string, number>), [purchaseOrders]);
  const enhancedItems = useMemo(() => items.map((item) => ({ ...item, incoming: Math.max(item.incoming, openPoBySku[item.sku] || 0), reserved: Math.max(item.reserved, consumptionBySku[item.sku] || 0), status: computeStatus({ ...item, incoming: Math.max(item.incoming, openPoBySku[item.sku] || 0), reserved: Math.max(item.reserved, consumptionBySku[item.sku] || 0) }) })), [items, consumptionBySku, openPoBySku]);
  const reorderSuggestions = useMemo(() => enhancedItems.map((item) => ({ item, jobDemand: consumptionBySku[item.sku] || 0, suggestedQty: reorderQty(item, consumptionBySku[item.sku] || 0) })).filter((row) => shouldReorder(row.item, row.jobDemand)), [enhancedItems, consumptionBySku]);
  const filtered = useMemo(() => { const text = query.trim().toLowerCase(); return enhancedItems.filter((item) => { const matchesText = !text || [item.sku, item.title, item.location, item.notes, csvJoin(item.linkedMaterialIds), item.preferredSupplier].join(' ').toLowerCase().includes(text); return matchesText && (statusFilter === 'All' || item.status === statusFilter) && (locationFilter === 'All' || item.locationType === locationFilter); }); }, [enhancedItems, query, statusFilter, locationFilter]);
  const activeItem = filtered.find((item) => item.id === activeId) ?? filtered[0] ?? enhancedItems[0] ?? null;
  useEffect(() => { if (activeItem && activeItem.id !== activeId) setActiveId(activeItem.id); }, [activeItem, activeId]);
  const stats = useMemo(() => { const critical = enhancedItems.filter((item) => item.status === 'Critical').length; const low = enhancedItems.filter((item) => item.status === 'Low').length; const inbound = enhancedItems.reduce((sum, item) => sum + item.incoming, 0); const available = enhancedItems.reduce((sum, item) => sum + Math.max(item.onHand - item.reserved, 0), 0); const reservedByJobs = consumption.reduce((sum, row) => sum + row.totalUnits, 0); const poValue = purchaseOrders.filter((po) => po.status !== 'received').reduce((sum, po) => sum + po.landedCostMinor, 0); return [{ label: 'Available units', value: available, note: 'Net of job reservations' }, { label: 'Low / critical SKUs', value: low + critical, note: `${critical} critical right now` }, { label: 'Reserved by jobs', value: reservedByJobs, note: `${consumption.length} material allocations` }, { label: 'Open PO value', value: money(poValue), note: `${purchaseOrders.filter((po) => po.status !== 'received').length} open purchase orders` }]; }, [enhancedItems, consumption, purchaseOrders]);

  function resetDraft() { setDraft(emptyDraft); setEditingId(null); }
  function saveItem() { if (!draft.sku.trim() || !draft.title.trim() || !draft.location.trim()) return; const nextBase = { ...draft, sku: draft.sku.trim().toUpperCase(), title: draft.title.trim(), location: draft.location.trim(), notes: draft.notes.trim(), linkedMaterialIds: draft.linkedMaterialIds || [], wastePercent: Number(draft.wastePercent || 0), unitCostMinor: Number(draft.unitCostMinor || 0), reorderMultiple: Number(draft.reorderMultiple || 1) }; const status = computeStatus(nextBase); if (editingId) { const next = items.map((item) => item.id === editingId ? { ...item, ...nextBase, status } : item); setItemsAndPersist(next, 'Saving inventory item through internal API...'); setActiveId(editingId); } else { const created: InventoryItem = { id: `inv-${Date.now()}`, ...nextBase, status }; const next = [created, ...items]; setItemsAndPersist(next, 'Creating inventory item through internal API...'); setActiveId(created.id); } resetDraft(); }
  function loadIntoForm(item: InventoryItem) { setEditingId(item.id); setDraft({ sku: item.sku, title: item.title, location: item.location, locationType: item.locationType, onHand: item.onHand, reserved: item.reserved, reorderPoint: item.reorderPoint, incoming: item.incoming, leadDays: item.leadDays, notes: item.notes, materialType: item.materialType || 'sheet', unit: item.unit || 'sheets', linkedMaterialIds: item.linkedMaterialIds || [], wastePercent: item.wastePercent || 0, preferredSupplier: item.preferredSupplier || '', unitCostMinor: item.unitCostMinor || 0, reorderMultiple: item.reorderMultiple || 1 }); }
  function patchItem(id: string, patch: Partial<InventoryItem>) { const next = items.map((item) => item.id === id ? { ...item, ...patch, status: computeStatus({ ...item, ...patch }) } : item); setItemsAndPersist(next, 'Saving inventory action through internal API...'); }
  function removeItem(id: string) { const next = items.filter((item) => item.id !== id); setItemsAndPersist(next, 'Deleting inventory item through internal API...'); if (activeId === id) setActiveId(next[0]?.id ?? ''); if (editingId === id) resetDraft(); }
  function reserveFromProduction() { const next = items.map((item) => { const reserved = consumptionBySku[item.sku] || item.reserved; const merged = { ...item, reserved: Math.max(item.reserved, reserved) }; return { ...merged, status: computeStatus(merged) }; }); setItemsAndPersist(next, 'Reserving materials from production tickets...'); }
  function consumeCompleted() { const dispatched = consumption.filter((row) => row.status === 'dispatched'); if (!dispatched.length) return; const usedBySku = dispatched.reduce((acc, row) => ({ ...acc, [row.materialSku]: (acc[row.materialSku] || 0) + row.totalUnits }), {} as Record<string, number>); const next = items.map((item) => { const used = usedBySku[item.sku] || 0; const merged = { ...item, onHand: Math.max(0, item.onHand - used), reserved: Math.max(0, item.reserved - used) }; return { ...merged, status: computeStatus(merged) }; }); setItemsAndPersist(next, 'Consuming materials from dispatched production tickets...'); }
  function resetSeed() { setItemsAndPersist(seedInventory, 'Resetting inventory records in database...'); setQuery(''); setStatusFilter('All'); setLocationFilter('All'); setActiveId(seedInventory[0]?.id ?? ''); resetDraft(); }
  function exportJson() { const blob = new Blob([JSON.stringify({ items: enhancedItems, consumption, purchaseOrders, reorderSuggestions, tickets }, null, 2)], { type: 'application/json' }); const url = URL.createObjectURL(blob); const link = document.createElement('a'); link.href = url; link.download = 'inventory-purchasing-v373.json'; link.click(); URL.revokeObjectURL(url); }
  function createPoForItem(item: InventoryItem, qty = reorderQty(item, consumptionBySku[item.sku] || 0)) { if (!qty) return; const po: PurchaseOrder = { id: `po-${Date.now()}-${item.sku}`, poNumber: `PO-${Date.now().toString().slice(-6)}`, supplier: item.preferredSupplier || 'Default Supplier', sku: item.sku, title: item.title, quantity: qty, unitCostMinor: item.unitCostMinor || 0, landedCostMinor: qty * Number(item.unitCostMinor || 0), status: 'draft', eta: eta(item.leadDays || 3), createdAt: new Date().toISOString(), notes: `Auto-created from v373 reorder suggestion. Job demand: ${consumptionBySku[item.sku] || 0}.` }; void persistPurchaseOrders([po, ...purchaseOrders], 'Creating purchase order draft...'); }
  function createAllSuggestedPos() { const newPos = reorderSuggestions.map(({ item, suggestedQty }) => ({ id: `po-${Date.now()}-${item.sku}`, poNumber: `PO-${Date.now().toString().slice(-6)}-${item.sku}`, supplier: item.preferredSupplier || 'Default Supplier', sku: item.sku, title: item.title, quantity: suggestedQty, unitCostMinor: item.unitCostMinor || 0, landedCostMinor: suggestedQty * Number(item.unitCostMinor || 0), status: 'draft' as const, eta: eta(item.leadDays || 3), createdAt: new Date().toISOString(), notes: `Auto-created from v373 batch reorder suggestion.` })); if (newPos.length) void persistPurchaseOrders([...newPos, ...purchaseOrders], 'Creating suggested purchase orders...'); }
  function updatePo(id: string, patch: Partial<PurchaseOrder>) { void persistPurchaseOrders(purchaseOrders.map((po) => po.id === id ? { ...po, ...patch } : po), 'Updating purchase order...'); }
  function receivePo(po: PurchaseOrder) { const nextItems = items.map((item) => item.sku === po.sku ? { ...item, onHand: item.onHand + po.quantity, incoming: Math.max(0, item.incoming - po.quantity), status: computeStatus({ ...item, onHand: item.onHand + po.quantity, incoming: Math.max(0, item.incoming - po.quantity) }) } : item); setItemsAndPersist(nextItems, 'Receiving purchase order into inventory...'); updatePo(po.id, { status: 'received' }); }

  return <div className="space-y-5">
    <PageHeader title="Inventory + Purchasing Automation" subtitle="Manage stock pools, job reservations, supplier reorders, purchase orders, inbound ETAs and material consumption from production demand." actions={<><Button onClick={loadInventory}>Refresh</Button><Button onClick={reserveFromProduction}>Reserve from jobs</Button><Button onClick={consumeCompleted}>Consume dispatched</Button><Button onClick={createAllSuggestedPos}>Create suggested POs</Button><Button onClick={resetSeed}>Reset seed</Button><Button onClick={exportJson}>Export JSON</Button><PrimaryButton onClick={saveItem}>{editingId ? 'Save inventory item' : 'Add inventory item'}</PrimaryButton></>} />
    <div className="rounded-xl border border-border bg-panel px-4 py-3 text-sm"><span className={dbStatus === 'connected' ? 'text-emerald-300' : dbStatus === 'error' ? 'text-red-300' : dbStatus === 'saving' ? 'text-amber-300' : dbStatus === 'local' ? 'text-amber-300' : 'text-textMuted'}>{dbStatus === 'connected' ? 'Database connected' : dbStatus === 'error' ? 'Database issue' : dbStatus === 'saving' ? 'Saving to database' : dbStatus === 'local' ? 'Local fallback' : 'Checking database'}</span><span className="ml-2 text-textMuted">{dbMessage}</span></div>
    <div className="grid gap-4 md:grid-cols-4">{stats.map((stat) => <Card key={stat.label} className="space-y-2"><p className="text-xs uppercase tracking-[0.2em] text-textMuted">{stat.label}</p><p className="text-3xl font-semibold text-text">{stat.value}</p><p className="text-sm text-textMuted">{stat.note}</p></Card>)}</div>
    <div className="grid gap-5 xl:grid-cols-[1.45fr_1fr]">
      <Card className="space-y-4"><div className="grid gap-3 md:grid-cols-4"><Input placeholder="Search SKU, supplier, material id" value={query} onChange={(e) => setQuery(e.target.value)} /><select className="rounded-xl border border-white/10 bg-white/[0.03] px-3 py-2 text-sm text-text outline-none" value={statusFilter} onChange={(e) => setStatusFilter(e.target.value as 'All' | StockStatus)}><option>All</option><option>Healthy</option><option>Low</option><option>Critical</option><option>Inbound</option></select><select className="rounded-xl border border-white/10 bg-white/[0.03] px-3 py-2 text-sm text-text outline-none" value={locationFilter} onChange={(e) => setLocationFilter(e.target.value as 'All' | LocationType)}><option>All</option><option>Warehouse</option><option>Plant</option><option>Materials</option></select><div className="rounded-xl border border-white/8 bg-white/[0.02] px-3 py-2 text-sm text-textMuted">{filtered.length} visible items</div></div><div className="space-y-3">{filtered.map((item) => { const available = Math.max(item.onHand - item.reserved, 0); const jobReserved = consumptionBySku[item.sku] || 0; const suggestion = reorderSuggestions.find((row) => row.item.sku === item.sku); return <button key={item.id} type="button" onClick={() => setActiveId(item.id)} className={`w-full rounded-2xl border p-4 text-left transition ${activeItem?.id === item.id ? 'border-accent/40 bg-accent/10' : 'border-white/8 bg-white/[0.02] hover:bg-white/[0.04]'}`}><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-sm font-semibold text-text">{item.sku} · {item.title}</p><p className="mt-1 text-sm text-textMuted">{item.location} · {item.preferredSupplier || 'No supplier'} · {item.unit || 'units'}</p></div><span className={`rounded-full border px-2.5 py-1 text-xs ${badgeClass(item.status)}`}>{item.status}</span></div><div className="mt-3 grid gap-2 text-xs text-textMuted sm:grid-cols-6"><span>On hand: <span className="text-text">{item.onHand}</span></span><span>Reserved: <span className="text-text">{item.reserved}</span></span><span>Job demand: <span className="text-text">{jobReserved}</span></span><span>Available: <span className="text-text">{available}</span></span><span>Incoming: <span className="text-text">{item.incoming}</span></span><span>Suggest: <span className="text-text">{suggestion?.suggestedQty || 0}</span></span></div></button>; })}</div></Card>
      <div className="space-y-5"><Card className="space-y-3"><p className="text-xs uppercase tracking-[0.2em] text-textMuted">Item editor</p><Input placeholder="SKU" value={draft.sku} onChange={(e) => setDraft((c) => ({ ...c, sku: e.target.value }))} /><Input placeholder="Item title" value={draft.title} onChange={(e) => setDraft((c) => ({ ...c, title: e.target.value }))} /><Input placeholder="Location" value={draft.location} onChange={(e) => setDraft((c) => ({ ...c, location: e.target.value }))} /><select className="rounded-xl border border-white/10 bg-white/[0.03] px-3 py-2 text-sm text-text outline-none" value={draft.locationType} onChange={(e) => setDraft((c) => ({ ...c, locationType: e.target.value as LocationType }))}><option>Warehouse</option><option>Plant</option><option>Materials</option></select><div className="grid grid-cols-2 gap-3"><Input type="number" placeholder="On hand" value={String(draft.onHand)} onChange={(e) => setDraft((c) => ({ ...c, onHand: toNumber(e.target.value) }))} /><Input type="number" placeholder="Reserved" value={String(draft.reserved)} onChange={(e) => setDraft((c) => ({ ...c, reserved: toNumber(e.target.value) }))} /><Input type="number" placeholder="Reorder point" value={String(draft.reorderPoint)} onChange={(e) => setDraft((c) => ({ ...c, reorderPoint: toNumber(e.target.value) }))} /><Input type="number" placeholder="Inbound" value={String(draft.incoming)} onChange={(e) => setDraft((c) => ({ ...c, incoming: toNumber(e.target.value) }))} /><Input type="number" placeholder="Lead days" value={String(draft.leadDays)} onChange={(e) => setDraft((c) => ({ ...c, leadDays: toNumber(e.target.value, 3) }))} /><Input type="number" placeholder="Waste %" value={String(draft.wastePercent || 0)} onChange={(e) => setDraft((c) => ({ ...c, wastePercent: toNumber(e.target.value) }))} /></div><div className="grid grid-cols-2 gap-3"><Input placeholder="Supplier" value={draft.preferredSupplier || ''} onChange={(e) => setDraft((c) => ({ ...c, preferredSupplier: e.target.value }))} /><Input type="number" placeholder="Unit cost pence" value={String(draft.unitCostMinor || 0)} onChange={(e) => setDraft((c) => ({ ...c, unitCostMinor: toNumber(e.target.value) }))} /><Input type="number" placeholder="Reorder multiple" value={String(draft.reorderMultiple || 1)} onChange={(e) => setDraft((c) => ({ ...c, reorderMultiple: toNumber(e.target.value, 1) }))} /><select className="rounded-xl border border-white/10 bg-white/[0.03] px-3 py-2 text-sm text-text outline-none" value={draft.unit} onChange={(e) => setDraft((c) => ({ ...c, unit: e.target.value as InventoryItem['unit'] }))}><option>sheets</option><option>meters</option><option>boards</option><option>units</option></select></div><Input placeholder="Linked material IDs, comma separated" value={csvJoin(draft.linkedMaterialIds)} onChange={(e) => setDraft((c) => ({ ...c, linkedMaterialIds: csv(e.target.value) }))} /><textarea className="min-h-[70px] rounded-xl border border-white/10 bg-white/[0.03] px-3 py-2 text-sm text-text outline-none placeholder:text-textMuted" placeholder="Operational notes, vendor context, substitution guidance..." value={draft.notes} onChange={(e) => setDraft((c) => ({ ...c, notes: e.target.value }))} /><div className="flex gap-2"><PrimaryButton onClick={saveItem}>{editingId ? 'Save changes' : 'Add item'}</PrimaryButton><Button onClick={resetDraft}>Clear</Button></div></Card>
      <Card className="space-y-4"><div className="flex items-center justify-between gap-3"><div><p className="text-xs uppercase tracking-[0.2em] text-textMuted">Inventory spotlight</p><p className="mt-1 text-lg font-semibold text-text">{activeItem ? activeItem.title : 'No item selected'}</p></div>{activeItem ? <span className={`rounded-full border px-2.5 py-1 text-xs ${badgeClass(activeItem.status)}`}>{activeItem.status}</span> : null}</div>{activeItem ? <><div className="grid gap-3 sm:grid-cols-2"><div className="rounded-2xl border border-white/8 bg-white/[0.02] p-3 text-sm text-textMuted"><p className="text-xs uppercase tracking-[0.18em] text-textMuted">Supply posture</p><p className="mt-2 text-text">Available {Math.max(activeItem.onHand - activeItem.reserved, 0)} / Reorder at {activeItem.reorderPoint}</p><p className="mt-1">Incoming {activeItem.incoming} · Lead time {activeItem.leadDays} days</p></div><div className="rounded-2xl border border-white/8 bg-white/[0.02] p-3 text-sm text-textMuted"><p className="text-xs uppercase tracking-[0.18em] text-textMuted">Purchasing</p><p className="mt-2 text-text">Supplier: {activeItem.preferredSupplier || 'Not set'}</p><p className="mt-1">Suggested reorder: {reorderQty(activeItem, consumptionBySku[activeItem.sku] || 0)} · Cost {money((activeItem.unitCostMinor || 0) * reorderQty(activeItem, consumptionBySku[activeItem.sku] || 0))}</p></div></div><div className="grid gap-2 sm:grid-cols-2"><Button onClick={() => createPoForItem(activeItem)}>Create PO draft</Button><Button onClick={() => patchItem(activeItem.id, { incoming: activeItem.incoming + Math.max(activeItem.reorderPoint, 50) })}>Raise replenishment</Button><Button onClick={() => patchItem(activeItem.id, { onHand: activeItem.onHand + activeItem.incoming, incoming: 0 })}>Receive inbound</Button><Button onClick={() => patchItem(activeItem.id, { reserved: Math.max(activeItem.reserved - 25, 0) })}>Release allocation</Button><Button onClick={() => loadIntoForm(activeItem)}>Edit item</Button><Button onClick={() => removeItem(activeItem.id)}>Delete item</Button></div></> : <p className="text-sm text-textMuted">Select an inventory item to view replenishment actions and supplier guidance.</p>}</Card></div>
    </div>
    <div className="grid gap-5 xl:grid-cols-2"><Card><div className="flex items-center justify-between gap-3"><div><h3 className="text-lg font-semibold text-white">Supplier reorder suggestions</h3><p className="mt-1 text-sm text-textMuted">Generated from available stock, incoming supply, reorder points and live job demand.</p></div><Button onClick={createAllSuggestedPos}>Create all POs</Button></div><div className="mt-4 space-y-3">{reorderSuggestions.map(({ item, suggestedQty, jobDemand }) => <div key={item.sku} className="rounded-2xl border border-white/8 bg-white/[0.03] p-4"><div className="flex items-start justify-between gap-3"><div><p className="font-semibold text-white">{item.sku} · {item.title}</p><p className="mt-1 text-sm text-textMuted">{item.preferredSupplier || 'Default Supplier'} · Lead {item.leadDays}d · Job demand {jobDemand}</p></div><span className="rounded-full border border-amber-500/20 bg-amber-500/10 px-3 py-1 text-xs text-amber-100">Order {suggestedQty}</span></div><div className="mt-3 flex flex-wrap gap-2"><Button onClick={() => createPoForItem(item, suggestedQty)}>Create PO draft</Button><span className="rounded-xl border border-white/8 bg-black/20 px-3 py-2 text-xs text-textMuted">ETA {eta(item.leadDays || 3)}</span><span className="rounded-xl border border-white/8 bg-black/20 px-3 py-2 text-xs text-textMuted">Landed {money((item.unitCostMinor || 0) * suggestedQty)}</span></div></div>)}{!reorderSuggestions.length ? <p className="rounded-2xl border border-dashed border-white/10 p-4 text-sm text-textMuted">No reorder suggestions right now.</p> : null}</div></Card>
    <Card><h3 className="text-lg font-semibold text-white">Purchase orders</h3><p className="mt-1 text-sm text-textMuted">Draft, sent, confirmed and received supplier POs.</p><div className="mt-4 overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="text-xs uppercase tracking-[0.16em] text-textMuted"><tr><th className="py-2 pr-4">PO</th><th className="py-2 pr-4">Supplier</th><th className="py-2 pr-4">SKU</th><th className="py-2 pr-4">Qty</th><th className="py-2 pr-4">Cost</th><th className="py-2 pr-4">ETA</th><th className="py-2 pr-4">Status</th><th className="py-2 pr-4">Action</th></tr></thead><tbody className="divide-y divide-white/8">{purchaseOrders.map((po) => <tr key={po.id} className="text-textMuted"><td className="py-3 pr-4 text-white">{po.poNumber}</td><td className="py-3 pr-4">{po.supplier}</td><td className="py-3 pr-4">{po.sku}</td><td className="py-3 pr-4">{po.quantity}</td><td className="py-3 pr-4">{money(po.landedCostMinor)}</td><td className="py-3 pr-4">{po.eta}</td><td className="py-3 pr-4"><span className={`rounded-full border px-2.5 py-1 text-xs ${badgeClass(po.status)}`}>{po.status}</span></td><td className="py-3 pr-4"><div className="flex gap-2"><Button onClick={() => updatePo(po.id, { status: po.status === 'draft' ? 'sent' : po.status === 'sent' ? 'confirmed' : po.status })}>Advance</Button><Button onClick={() => receivePo(po)}>Receive</Button></div></td></tr>)}{!purchaseOrders.length ? <tr><td colSpan={8} className="py-5 text-textMuted">No purchase orders yet.</td></tr> : null}</tbody></table></div></Card></div>
    <Card><h3 className="text-lg font-semibold text-white">Production material consumption plan</h3><p className="mt-1 text-sm text-textMuted">Calculated from live production job tickets. Matching uses ticket material against SKU/title/linked material IDs.</p><div className="mt-4 overflow-x-auto"><table className="w-full min-w-[900px] text-left text-sm"><thead className="text-xs uppercase tracking-[0.16em] text-textMuted"><tr><th className="py-2 pr-4">Order</th><th className="py-2 pr-4">Product</th><th className="py-2 pr-4">Material SKU</th><th className="py-2 pr-4">Qty</th><th className="py-2 pr-4">Base use</th><th className="py-2 pr-4">Waste</th><th className="py-2 pr-4">Total reserve</th><th className="py-2 pr-4">Status</th></tr></thead><tbody className="divide-y divide-white/8">{consumption.map((row) => <tr key={`${row.ticketId}-${row.materialSku}`} className="text-textMuted"><td className="py-3 pr-4 text-white">{row.orderNumber}</td><td className="py-3 pr-4">{row.productName}</td><td className="py-3 pr-4">{row.materialSku}</td><td className="py-3 pr-4">{row.quantity}</td><td className="py-3 pr-4">{row.consumeUnits}</td><td className="py-3 pr-4">{row.wasteUnits}</td><td className="py-3 pr-4 text-white">{row.totalUnits}</td><td className="py-3 pr-4">{row.status}</td></tr>)}{!consumption.length ? <tr><td colSpan={8} className="py-5 text-textMuted">No material consumption matched yet. Link inventory material IDs to ticket materials.</td></tr> : null}</tbody></table></div></Card>
  </div>;
}
