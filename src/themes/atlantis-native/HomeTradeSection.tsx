import { Check } from 'lucide-react';
import { BRAND, storeHref } from './theme-helpers';
import { PrimaryButton, SecondaryButton, Shell } from './HomePrimitives';

export default function HomeTradeSection({ storeBase }: { storeBase: string }) {
  return <section className="py-8"><Shell><div className="overflow-hidden rounded-[30px] border shadow-[0_24px_70px_rgba(0,0,0,0.08)]" style={{ borderColor: BRAND.line, background: `linear-gradient(135deg, ${BRAND.primary} 0%, ${BRAND.primaryDark} 55%, #EFFBFE 55%, #FFFFFF 100%)` }}><div className="grid gap-8 p-8 md:grid-cols-[1fr_1.1fr] md:p-10 lg:p-12"><div className="flex flex-col justify-center text-white"><div className="text-[10px] font-black uppercase tracking-[0.18em] text-white/75">New trade customer?</div><h2 className="mt-3 max-w-[560px] text-[36px] font-black leading-[1] tracking-[-0.05em]">Business print support for repeat orders and larger projects.</h2><p className="mt-4 max-w-[560px] text-[13px] leading-7 text-white/85">Prepare the homepage for trade accounts, regular reorder customers, local designers, schools, charities, restaurants and event organisers.</p><div className="mt-6 flex flex-wrap gap-3"><SecondaryButton href={storeHref(storeBase, '/bespoke-quote')}>Request trade quote</SecondaryButton></div></div><div className="grid gap-4 sm:grid-cols-2">{['Repeat ordering','Bulk quote support','Artwork help','Local collection'].map((item) => <div key={item} className="rounded-[22px] border bg-white/88 p-5 shadow-[0_16px_40px_rgba(0,0,0,0.08)]" style={{ borderColor: 'rgba(255,255,255,0.65)' }}><Check className="h-5 w-5" style={{ color: BRAND.primary }} /><div className="mt-4 text-[16px] font-black" style={{ color: BRAND.ink }}>{item}</div><p className="mt-2 text-[12px] leading-6" style={{ color: BRAND.muted }}>Ready to connect into your admin dashboard later.</p></div>)}</div></div></div></Shell></section>;
}
