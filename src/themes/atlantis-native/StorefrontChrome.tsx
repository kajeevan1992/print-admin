import Link from 'next/link';
import { ChevronDown, ChevronRight, MapPin, Search, ShoppingCart, User } from 'lucide-react';
import type { NavItem } from './types';
import { BRAND, storeHref } from './theme-helpers';

function Shell({ children, narrow = false }: { children: React.ReactNode; narrow?: boolean }) {
  return <div className={`mx-auto w-full px-4 sm:px-6 lg:px-8 ${narrow ? 'max-w-[1220px]' : 'max-w-[1360px]'}`}>{children}</div>;
}

function IconButton({ icon }: { icon: React.ReactNode }) {
  return <div className="grid h-9 w-9 place-items-center rounded-xl border bg-white" style={{ borderColor: BRAND.line }}>{icon}</div>;
}

function UtilityBar() {
  return <div style={{ backgroundColor: BRAND.black, color: 'white' }}><Shell><div className="flex h-8 items-center justify-between text-[11px] font-medium"><span>Professional print, same day printing, signage and packaging solutions</span><div className="hidden gap-5 sm:flex"><span>Business orders</span><span>Bulk pricing</span><span>Fast turnaround</span><span>Bespoke quote support</span></div></div></Shell></div>;
}

function Header({ currentPath = '/', navItems, storeBase }: { currentPath?: string; navItems: NavItem[]; storeBase: string }) {
  return <header className="sticky top-0 z-40 border-b bg-white/95 backdrop-blur transition-all duration-300" style={{ borderColor: BRAND.line }}><Shell><div className="relative"><div className="grid h-[74px] grid-cols-[auto_1fr_auto] items-center gap-6"><div className="flex items-center gap-3"><Link href={storeBase} className="flex items-center gap-0.5 no-underline"><span className="text-[42px] font-black tracking-[-0.055em]" style={{ color: BRAND.primary }}>HOLO</span><span className="text-[42px] font-black tracking-[-0.055em]" style={{ color: BRAND.ink }}>PRINT</span></Link></div><nav className="hidden items-center justify-center gap-4 xl:flex">{navItems.map((item) => { const active = currentPath === item.path || currentPath.startsWith(`${item.path}/`); return <div className="group flex h-[74px] items-center" key={item.label}><Link href={storeHref(storeBase, item.path)} className="inline-flex items-center gap-1 text-[13px] font-semibold tracking-[-0.01em] no-underline" style={{ color: active ? BRAND.primary : BRAND.ink }}>{item.label}<ChevronDown className="h-4 w-4 transition group-hover:rotate-180" /></Link><div className="absolute left-0 right-0 top-full hidden group-hover:block"><div className="mt-2 rounded-[22px] border bg-white p-5 shadow-[0_34px_100px_rgba(0,0,0,0.13)]" style={{ borderColor: BRAND.line }}><div className="grid gap-5"><div className="grid grid-cols-[270px_1fr_1fr_1fr] gap-6"><div className="rounded-[20px] border p-4" style={{ borderColor: BRAND.line, background: 'linear-gradient(180deg, #FBFDFE 0%, #F4F9FB 100%)' }}><img src={item.feature.image} alt={item.feature.title} className="h-36 w-full rounded-[12px] object-cover" /><div className="mt-4 text-[18px] font-black tracking-[-0.03em]" style={{ color: BRAND.ink }}>{item.feature.title}</div><p className="mt-2 text-[12px] leading-6" style={{ color: BRAND.muted }}>{item.feature.body}</p><Link href={storeHref(storeBase, item.path)} className="mt-4 inline-block text-[12px] font-bold no-underline" style={{ color: BRAND.primary }}>{item.feature.cta}</Link></div>{item.columns.map((column) => <div key={column.title}><div className="mb-2 text-[10px] font-bold uppercase tracking-[0.18em]" style={{ color: BRAND.primary }}>{column.title}</div><div className="grid gap-1">{column.links.map(([label, path]) => <Link key={`${label}-${path}`} href={storeHref(storeBase, path)} className="rounded-xl px-3 py-2 text-left text-[12px] font-medium no-underline hover:bg-[#F6F7F8]" style={{ color: BRAND.ink }}>{label}</Link>)}</div></div>)}</div><div className="grid grid-cols-4 gap-3 border-t pt-4" style={{ borderColor: BRAND.line }}>{['Fast turnaround', 'Premium stock', 'Bulk pricing', 'Artwork support'].map((itemText) => <div key={itemText} className="rounded-[16px] border px-4 py-3 text-[11px] font-semibold" style={{ borderColor: BRAND.line, color: BRAND.muted, background: 'linear-gradient(180deg, #FFFFFF 0%, #F8FBFC 100%)' }}>{itemText}</div>)}</div></div></div></div></div>; })}</nav><div className="ml-auto flex items-center gap-2"><Link href={`${storeBase}/collection-points`} className="hidden h-9 items-center gap-2 rounded-xl border bg-white px-4 text-[12px] font-black no-underline lg:inline-flex" style={{ borderColor: BRAND.line, color: BRAND.ink }}><MapPin className="h-4 w-4" style={{ color: BRAND.primary }} /><span>Select store</span></Link><button><IconButton icon={<Search className="h-4 w-4" />} /></button><Link href={`${storeBase}/login`}><IconButton icon={<User className="h-4 w-4" />} /></Link><Link href={`${storeBase}/cart`} className="flex items-center gap-2 rounded-xl border px-3 py-2 text-[12px] font-semibold no-underline" style={{ borderColor: BRAND.line, color: BRAND.muted, backgroundColor: 'white' }}><ShoppingCart className="h-4 w-4" /><span>£0.00</span></Link></div></div></div></Shell></header>;
}

function FooterCol({ title, items, storeBase }: { title: string; items: [string, string][]; storeBase: string }) {
  return <div><div className="mb-3 text-[12px] font-bold uppercase tracking-[0.16em]" style={{ color: BRAND.ink }}>{title}</div><div className="grid gap-2">{items.map(([label, path]) => <Link key={label} href={storeHref(storeBase, path)} className="text-left text-[12px] no-underline" style={{ color: BRAND.muted }}>{label}</Link>)}</div></div>;
}

function Footer({ storeBase }: { storeBase: string }) {
  return <footer className="mt-8 border-t bg-white" style={{ borderColor: BRAND.line }}><div className="border-b py-3" style={{ borderColor: BRAND.line, backgroundColor: BRAND.primary }}><Shell><div className="flex flex-col items-center justify-between gap-3 text-[12px] font-semibold text-white md:flex-row"><span>Get the very best print solutions for your business, events and brand campaigns — with room to grow into a full admin-connected storefront.</span><div className="flex gap-2"><input className="h-9 w-[250px] rounded-full border-0 bg-white px-4 text-[12px] text-black outline-none" placeholder="Email address" /><button className="rounded-full bg-black px-4 text-[12px] font-bold text-white">Subscribe</button></div></div></Shell></div><Shell><div className="grid gap-3 py-5 md:grid-cols-4">{[['Business printing','20+'],['Event signage','12+'],['Labels & packaging','18+'],['Custom quote support','1:1']].map(([item,count]) => <div key={item} className="rounded-[18px] border px-4 py-3" style={{ borderColor: BRAND.line, color: BRAND.muted, background: 'linear-gradient(180deg, #FFFFFF 0%, #F8FBFC 100%)' }}><div className="text-[10px] font-bold uppercase tracking-[0.14em]">{item}</div><div className="mt-1 text-[16px] font-black" style={{ color: BRAND.ink }}>{count}</div></div>)}</div><div className="grid gap-8 py-10 md:grid-cols-[1.25fr_0.8fr_0.8fr_0.8fr_0.8fr]"><div><Link href={storeBase} className="flex items-center gap-0.5 no-underline"><span className="text-[50px] font-black tracking-[-0.055em]" style={{ color: BRAND.primary }}>HOLO</span><span className="text-[50px] font-black tracking-[-0.055em]" style={{ color: BRAND.ink }}>PRINT</span></Link><p className="mt-4 max-w-[360px] text-[12px] leading-7" style={{ color: BRAND.muted }}>A fuller ecommerce print storefront direction with broader navigation, denser sections and a cleaner visual tone.</p></div><FooterCol storeBase={storeBase} title="Products" items={[['Business Cards','/business-cards'],['Flyers','/flyers'],['Posters','/posters-large-format-prints'],['Booklets','/booklets']]} /><FooterCol storeBase={storeBase} title="Categories" items={[['Labels','/all-products'],['Stationery','/stationery'],['Signage','/signage'],['Packaging','/all-products']]} /><FooterCol storeBase={storeBase} title="Business" items={[['Bulk pricing','/bespoke-quote'],['Custom quotes','/bespoke-quote'],['Artwork advice','/bespoke-quote'],['Delivery support','/all-products']]} /><FooterCol storeBase={storeBase} title="Support" items={[['All products','/all-products'],['Cart','/cart'],['Contact','/bespoke-quote'],['Quote request','/bespoke-quote']]} /></div></Shell></footer>;
}

export default function StorefrontChrome({ currentPath = '/', children, navItems, storeBase }: { currentPath?: string; children: React.ReactNode; navItems: NavItem[]; storeBase: string }) {
  return <div style={{ backgroundColor: BRAND.bg, color: BRAND.ink }}><UtilityBar /><Header currentPath={currentPath} navItems={navItems} storeBase={storeBase} />{children}<Footer storeBase={storeBase} /></div>;
}
