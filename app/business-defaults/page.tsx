export const dynamic = 'force-dynamic';

import { DefaultStoreBootstrapPage } from '@/modules/platform/default-store-bootstrap-page';

export default function Page() {
  return <DefaultStoreBootstrapPage />;
}
