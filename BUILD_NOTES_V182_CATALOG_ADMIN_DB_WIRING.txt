V182 - Catalog admin DB wiring fix

Scope kept to catalog backend/data wiring. No UI redesign and no public API changes.

Changes:
- Products admin data service now reads from /api/internal/catalog/products instead of local demo/localStorage data in browser runtime.
- Product create/update/delete/clone now write through existing internal catalog products endpoint.
- Categories admin data service now reads from /api/internal/catalog/categories instead of local demo/localStorage data in browser runtime.
- Category create/update/delete/toggle now write through existing internal catalog categories endpoint.
- Added internal-only uppercase compatibility route /api/internal/catalog/Categories that re-exports the existing categories handler, so the URL reported as missing resolves without creating a new public API.
- Kept server-side/mock fallback only for non-browser runtime safety; browser/admin runtime now uses internal core DB endpoints.

Validation:
- Full build could not be run in this environment because node_modules are not installed and prisma CLI is unavailable.
- npm run build fails immediately with: sh: 1: prisma: not found.
