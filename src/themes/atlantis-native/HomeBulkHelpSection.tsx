import { BRAND, storeHref } from './theme-helpers';
import { SecondaryButton, SectionHeading, Shell } from './HomePrimitives';
import { helpBlogs } from './home-extra-data';

export default function HomeBulkHelpSection({ storeBase }: { storeBase: string }) {
  return <><section className="py-8" style={{ background: 'linear-gradient(180deg,#FFFFFF 0%,#F3F7FA 100%)' }}><Shell><div className="grid gap-8 lg:grid-cols-[1fr_360px] lg:items-center"><div><div className="text-[10px] font-black uppercase tracking-[0.18em]" style={{ color: BRAND.primary }}>Print more, save more</div><h2 className="mt-3 text-[36px] font-black tracking-[-0.05em]" style={{ color: BRAND.ink }}>Bulk, repeat and quote-friendly ordering blocks.</h2><p className="mt-4 max-w-[760px] text-[13px] leading-7" style={{ color: BRAND.muted }}>Use the homepage to push customers toward higher value orders: repeat business cards, event bundles, signage packs, seasonal campaigns and bespoke quote requests.</p><div className="mt-6"><SecondaryButton href={storeHref(storeBase, '/all-products')}>Browse categories</SecondaryButton></div></div><div className="rounded-[28px] border bg-white p-6 text-center shadow-[0_18px_44px_rgba(0,0,0,0.06)]" style={{ borderColor: BRAND.line }}><div className="text-[58px] font-black tracking-[-0.08em]" style={{ color: BRAND.primary }}>B2B</div><p className="mt-2 text-[13px] leading-6" style={{ color: BRAND.muted }}>Built for repeat customers, trade support and account workflows later.</p></div></div></Shell></section><section className="py-8"><Shell><SectionHeading eyebrow="Trending help" title="Print tips, product guidance and ideas" /><div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{helpBlogs.map((item) => <div key={item.title} className="rounded-[22px] border bg-white p-4 shadow-[0_14px_30px_rgba(0,0,0,0.04)]" style={{ borderColor: BRAND.line }}><img src={item.image} alt={item.title} className="h-40 w-full rounded-[16px] object-cover" /><div className="mt-4 text-[15px] font-black" style={{ color: BRAND.ink }}>{item.title}</div><p className="mt-2 text-[12px] leading-6" style={{ color: BRAND.muted }}>{item.text}</p></div>)}</div></Shell></section></>;
}
