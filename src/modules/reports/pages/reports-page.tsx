'use client';

import { useEffect, useMemo, useState } from 'react';
import { PageHeader } from '@/components/ui/page-header';
import { Card } from '@/components/ui/card';
import { DataTable } from '@/components/data-table/data-table';
import { Button } from '@/components/ui/buttons';
import { reportsService } from '@/services/reports.service';

type RevenuePoint = Awaited<ReturnType<typeof reportsService.getRevenueSeries>>[number];
type ChannelPerformance = Awaited<ReturnType<typeof reportsService.getChannelPerformance>>[number];
type ProductPerformance = Awaited<ReturnType<typeof reportsService.getProductPerformance>>[number];
const dateRanges = ['Last 7 days', 'Last 30 days', 'This month', 'Quarter to date'];
function downloadCsv(filename: string, rows: Array<Record<string, unknown>>) { const headers = Array.from(new Set(rows.flatMap((row) => Object.keys(row)))); const csv = [headers.join(','), ...rows.map((row) => headers.map((key) => JSON.stringify(row[key] ?? '')).join(','))].join('\n'); const blob = new Blob([csv], { type: 'text/csv;charset=utf-8' }); const url = URL.createObjectURL(blob); const a = document.createElement('a'); a.href = url; a.download = filename; a.click(); URL.revokeObjectURL(url); }
export function ReportsPage() {
  const [range, setRange] = useState(dateRanges[0]);
  const [revenue, setRevenue] = useState<RevenuePoint[]>([]);
  const [channels, setChannels] = useState<ChannelPerformance[]>([]);
  const [products, setProducts] = useState<ProductPerformance[]>([]);
  const [loading, setLoading] = useState(true);
  const [message, setMessage] = useState('');
  async function load() { setLoading(true); const [revenueData, channelData, productData] = await Promise.all([reportsService.getRevenueSeries(), reportsService.getChannelPerformance(), reportsService.getProductPerformance()]); setRevenue(revenueData); setChannels(channelData); setProducts(productData); setLoading(false); }
  useEffect(() => { void load(); }, []);
  const totals = useMemo(() => { const revenueTotal = revenue.reduce((sum, item) => sum + item.revenue, 0); const ordersTotal = revenue.reduce((sum, item) => sum + item.orders, 0); return { revenueTotal, ordersTotal, avgOrderValue: ordersTotal ? revenueTotal / ordersTotal : 0 }; }, [revenue]);
  function exportCsv() { downloadCsv('tenant-reports.csv', [...revenue.map((row) => ({ type: 'revenue', ...row })), ...channels.map((row) => ({ type: 'channel', ...row })), ...products.map((row) => ({ type: 'product', ...row }))]); setMessage('CSV exported from live report data.'); }
  return <div><PageHeader title="Reports" subtitle="Track commercial performance, channel contribution, and top-selling products from live tenant data." actions={<><Button onClick={exportCsv}>Export CSV</Button><Button onClick={() => setMessage('Scheduled reports are not enabled yet. Use Export CSV for now.')}>Schedule Report</Button><Button onClick={() => void load()}>Refresh</Button></>} />{message ? <div className="mb-4 rounded-xl border border-white/10 bg-white/[0.04] p-3 text-sm text-textMuted">{message}</div> : null}<div className="mb-6 flex flex-wrap gap-2">{dateRanges.map((item) => <button key={item} onClick={() => setRange(item)} className={`rounded-lg border px-3 py-2 text-sm ${range === item ? 'border-accent bg-panelMuted text-text' : 'border-border text-textMuted'}`}>{item}</button>)}</div>{loading ? <Card>Loading reports...</Card> : <><div className="mb-6 grid gap-4 md:grid-cols-3"><Card><p className="text-xs uppercase text-textMuted">Revenue</p><p className="mt-2 text-2xl font-semibold">£{Math.round(totals.revenueTotal).toLocaleString()}</p><p className="mt-1 text-sm text-textMuted">{range}</p></Card><Card><p className="text-xs uppercase text-textMuted">Orders</p><p className="mt-2 text-2xl font-semibold">{totals.ordersTotal.toLocaleString()}</p><p className="mt-1 text-sm text-textMuted">Live order records</p></Card><Card><p className="text-xs uppercase text-textMuted">Average order value</p><p className="mt-2 text-2xl font-semibold">£{Math.round(totals.avgOrderValue).toLocaleString()}</p><p className="mt-1 text-sm text-textMuted">Across live orders</p></Card></div><Card className="mb-6"><div className="mb-4"><h3 className="text-sm font-semibold">Revenue trend</h3><p className="text-sm text-textMuted">Daily snapshot from live orders.</p></div><div className="grid gap-3 md:grid-cols-7">{revenue.map((item) => <div key={item.label} className="rounded-xl border border-border bg-panelMuted p-3"><p className="text-xs uppercase text-textMuted">{item.label}</p><p className="mt-2 text-lg font-semibold">£{Math.round(item.revenue).toLocaleString()}</p><p className="text-sm text-textMuted">{item.orders} orders</p></div>)}{!revenue.length ? <p className="text-sm text-textMuted">No live order revenue yet.</p> : null}</div></Card><div className="mb-6 grid gap-6 xl:grid-cols-2"><div><h3 className="mb-3 text-sm font-semibold">Channel performance</h3><DataTable columns={[{ key: 'channel', header: 'Channel', render: (row) => row.channel }, { key: 'orders', header: 'Orders', render: (row) => row.orders.toLocaleString() }, { key: 'revenue', header: 'Revenue', render: (row) => `£${Math.round(row.revenue).toLocaleString()}` }, { key: 'conversion', header: 'Conversion', render: (row) => row.conversion }, { key: 'aov', header: 'AOV', render: (row) => row.aov }]} rows={channels} rowKey={(row) => row.id} /></div><div><h3 className="mb-3 text-sm font-semibold">Products</h3><DataTable columns={[{ key: 'product', header: 'Product', render: (row) => row.product }, { key: 'category', header: 'Category', render: (row) => row.category }, { key: 'orders', header: 'Orders', render: (row) => row.orders.toLocaleString() }, { key: 'revenue', header: 'Revenue', render: (row) => `£${row.revenue.toLocaleString()}` }, { key: 'margin', header: 'Margin', render: (row) => row.margin }]} rows={products} rowKey={(row) => row.id} /></div></div></>}</div>;
}
