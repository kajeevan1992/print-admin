import StorefrontChrome from './StorefrontChrome';
import type { NavItem } from './types';
import type { ThemeProductCard } from './catalog-adapter';
import { BRAND } from './theme-helpers';
import { Shell } from './HomePrimitives';

function titleFromSlug(value: string) { return String(value || '').split('-').filter(Boolean).map((word) => word.charAt(0).toUpperCase() + word.slice(1)).join(' '); }
function selectedOptionRows(product: ThemeProductCard | undefined, searchParams: Record<string, string>) { if (!product?.optionGroups?.length) return []; return product.optionGroups.map((group) => { const selectedSlug = searchParams[group.key] || group.values[0]?.slug || ''; const selected = group.values.find((value) => value.slug === selectedSlug); return selected ? { key: group.key, label: group.label, value: selected.label, slug: selected.slug } : null; }).filter(Boolean) as { key: string; label: string; value: string; slug: string }[]; }
function optionQuery(rows: { key: string; slug: string }[]) { const params = new URLSearchParams(); rows.forEach((row) => { if (row.key && row.slug) params.set(row.key, row.slug); }); const query = params.toString(); return query ? `?${query}` : ''; }

export default function CartPage({ tenantSlug = '', storeSlug = '', storeBase, navItems, productSlug, categorySlug, products = [], searchParams = {} }: { tenantSlug?: string; storeSlug?: string; storeBase: string; navItems: NavItem[]; productSlug?: string; categorySlug?: string; products?: ThemeProductCard[]; searchParams?: Record<string, string> }) {
  const product = products.find((item) => item.slug === productSlug && (!categorySlug || item.category === categorySlug));
  const title = product?.title || titleFromSlug(productSlug || '') || 'Your cart';
  const optionRows = selectedOptionRows(product, searchParams);
  const configuredProductHref = product ? `${storeBase}/${product.category}/${product.slug}${optionQuery(optionRows)}` : storeBase;
  const priceMinor = Number(product?.priceFromMinor || 0);
  return <StorefrontChrome currentPath="/cart" navItems={navItems} storeBase={storeBase}><section className="py-10"><Shell><div className="rounded-[32px] border bg-white p-8 shadow-sm" style={{ borderColor: BRAND.line }}><div className="text-[11px] font-black uppercase tracking-[0.18em]" style={{ color: BRAND.primary }}>Cart</div><h1 className="mt-4 text-[38px] font-black tracking-[-0.055em]" style={{ color: BRAND.ink }}>{product ? `${title} added to cart` : 'Your cart'}</h1><p className="mt-3 max-w-[720px] text-sm leading-7" style={{ color: BRAND.muted }}>{product ? 'Complete your details to create the order and continue to Stripe payment.' : 'Cart is ready for the next checkout connection step.'}</p>{product ? <div className="mt-6 rounded-[24px] border p-5" style={{ borderColor: BRAND.line }}><div className="text-[18px] font-black" style={{ color: BRAND.ink }}>{product.title}</div><div className="mt-1 text-sm font-bold" style={{ color: BRAND.primary }}>{product.price}</div><div className="mt-3 text-sm" style={{ color: BRAND.muted }}>Category: {product.category}</div>{optionRows.length ? <div className="mt-5 grid gap-3 sm:grid-cols-2">{optionRows.map((row) => <div key={row.key} className="rounded-[18px] border p-4" style={{ borderColor: BRAND.line }}><div className="text-[11px] font-black uppercase tracking-[0.14em]" style={{ color: BRAND.muted }}>{row.label}</div><div className="mt-1 text-sm font-black" style={{ color: BRAND.ink }}>{row.value}</div></div>)}</div> : null}</div> : null}{product ? <form action="/api/native-storefront/checkout" method="post" className="mt-6 rounded-[24px] border p-5" style={{ borderColor: BRAND.line }}><input type="hidden" name="tenantSlug" value={tenantSlug} /><input type="hidden" name="storeSlug" value={storeSlug} /><input type="hidden" name="productSlug" value={product.slug} /><input type="hidden" name="categorySlug" value={product.category} /><input type="hidden" name="productTitle" value={product.title} /><input type="hidden" name="unitPriceMinor" value={priceMinor} /><input type="hidden" name="selectedOptions" value={JSON.stringify(optionRows)} /><div className="text-[18px] font-black" style={{ color: BRAND.ink }}>Checkout details</div><div className="mt-4 grid gap-4 sm:grid-cols-2"><input required name="customerName" placeholder="Name" className="rounded-2xl border px-4 py-3 text-sm" style={{ borderColor: BRAND.line }} /><input required name="customerEmail" type="email" placeholder="Email" className="rounded-2xl border px-4 py-3 text-sm" style={{ borderColor: BRAND.line }} /><input name="quantity" type="number" min="1" defaultValue="1" className="rounded-2xl border px-4 py-3 text-sm" style={{ borderColor: BRAND.line }} /><select name="artworkStatus" defaultValue="send-later" className="rounded-2xl border px-4 py-3 text-sm" style={{ borderColor: BRAND.line }}><option value="ready">Artwork ready</option><option value="send-later">Send artwork later</option><option value="need-design">Need design help</option></select></div>{priceMinor > 0 ? <button className="mt-5 w-full rounded-full px-5 py-3 text-[12px] font-black text-white" style={{ backgroundColor: BRAND.primary }}>Continue to Stripe payment</button> : <div className="mt-5 rounded-[18px] border p-4 text-sm" style={{ borderColor: BRAND.line, color: BRAND.muted }}>Add a fixed price in SaaS admin before online payment can start.</div>}<p className="mt-4 text-[12px]" style={{ color: BRAND.muted }}>VAT is off for HOLO launch because the store is not VAT registered yet.</p></form> : null}<div className="mt-7 flex flex-wrap gap-3"><a href={storeBase} className="rounded-full px-5 py-3 text-[12px] font-black text-white no-underline" style={{ backgroundColor: BRAND.primary }}>Continue shopping</a>{product ? <a href={configuredProductHref} className="rounded-full border px-5 py-3 text-[12px] font-black no-underline" style={{ borderColor: BRAND.line, color: BRAND.ink }}>Edit options</a> : null}</div></div></Shell></section></StorefrontChrome>;
}
